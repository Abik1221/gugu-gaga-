# Application
ENVIRONMENT=development
DEBUG=true
APP_NAME=Zemen Pharma
CORS_ORIGINS=

# Database (to be set later)
DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/zemen_pharma

# Redis (to be set later)
REDIS_URL=redis://localhost:6379/0

# Gemini API (to be set later)
GEMINI_API_KEY=

# Auth / JWT
JWT_SECRET=changeme_in_production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRES_MINUTES=30

# Rate limits
RATE_LIMIT_GENERAL_PER_MINUTE=60
RATE_LIMIT_GEMINI_PER_MINUTE=30

# Email (SMTP)
EMAIL_ENABLED=true
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=nahomkeneni4@gmail.com
SMTP_PASSWORD=use_app_password_from_google
SMTP_USE_TLS=true
EMAIL_FROM=nahomkeneni4@gmail.com

# Admin User (Auto-created on startup)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=your_secure_password
